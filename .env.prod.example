# Used by docker-compose.prod.yml for the domain Caddy should get a TLS
# certificate for, and for the backend's CORS allow-list.
#
# Copy this to .env (same folder) and set your real domain:
#   cp .env.prod.example .env
DOMAIN=docbot.example.com
